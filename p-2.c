#include <stdio.h>

void main()
{
    float first, second;
    scanf("%f, %f", &first, &second);
    if (first = second)
    {
        printf("both numbers are equal");
    }
    else
    {
        printf("Both numbers are not equal");
    }
}